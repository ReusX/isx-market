import { AboutPage } from '@/components/site/InfoPages'

// Title/description live in ./layout.tsx. The surface is shared with
// app/(en)/en/about — see components/routes/AboutPage.tsx.
export default function Page() {
  return <AboutPage />
}
